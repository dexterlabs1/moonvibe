{
  "_comment": [
    "Shot definitions for offscreen review captures. One process per shot:",
    "  MOONVIBE_SHOOT=<name> MOONVIBE_SHOOT_OUT=<dir> ./moonvibe",
    "tools/shoot/review.sh runs all of them, then the design linter over the dumps.",
    "",
    "  view      what main.qml starts on (default qrc:/gui/PcView.qml)",
    "  fixtures  default (three hosts) | single | none",
    "  steps     key: a QKeySequence string, sent to the window",
    "            eval: QML evaluated in main.qml's scope, so ids like stackView work",
    "            click: [x, y] in window coordinates",
    "            waitMs: pause after this step (default 150)",
    "  settleMs  pause before the first step and before the grab",
    "",
    "Adding a shot needs no rebuild. Prefer eval over long key sequences: a shot",
    "that navigates by pressing Down eleven times breaks the day a row is added."
  ],

  "shots": {
    "hosts": {
      "view": "qrc:/gui/PcView.qml",
      "fixtures": "default"
    },

    "hosts-empty": {
      "view": "qrc:/gui/PcView.qml",
      "fixtures": "none",
      "settleMs": 900
    },

    "hosts-focused": {
      "view": "qrc:/gui/PcView.qml",
      "fixtures": "default",
      "steps": [
        { "key": "Down", "waitMs": 250 }
      ]
    },

    "host-menu": {
      "view": "qrc:/gui/PcView.qml",
      "fixtures": "default",
      "steps": [
        { "key": "Down", "waitMs": 250 },
        { "key": "Menu", "waitMs": 400 }
      ]
    },

    "library": {
      "view": "qrc:/gui/PcView.qml",
      "fixtures": "single",
      "steps": [
        {
          "eval": "var c = Qt.createComponent('qrc:/gui/AppView.qml'); stackView.push(c.createObject(stackView, {'computerIndex': 0, 'objectName': 'TOWER'}))",
          "waitMs": 900
        }
      ],
      "settleMs": 700
    },

    "library-focused": {
      "view": "qrc:/gui/PcView.qml",
      "fixtures": "single",
      "steps": [
        {
          "eval": "var c = Qt.createComponent('qrc:/gui/AppView.qml'); stackView.push(c.createObject(stackView, {'computerIndex': 0, 'objectName': 'TOWER'}))",
          "waitMs": 900
        },
        { "key": "Right", "waitMs": 300 }
      ],
      "settleMs": 700
    },

    "settings": {
      "view": "qrc:/gui/PcView.qml",
      "fixtures": "default",
      "steps": [
        { "eval": "settingsButton.clicked()", "waitMs": 900 }
      ],
      "settleMs": 700
    },

    "settings-audio": {
      "_note": "D-pad Down on the rail moves the selection to Audio and swaps the pane live.",
      "view": "qrc:/gui/PcView.qml",
      "fixtures": "default",
      "steps": [
        { "eval": "settingsButton.clicked()", "waitMs": 900 },
        { "key": "Down", "waitMs": 300 }
      ],
      "settleMs": 700
    },

    "settings-pane": {
      "_note": "Right enters the Display pane (focus on the first control); Tab travels to the next.",
      "view": "qrc:/gui/PcView.qml",
      "fixtures": "default",
      "steps": [
        { "eval": "settingsButton.clicked()", "waitMs": 900 },
        { "key": "Right", "waitMs": 250 },
        { "key": "Tab", "waitMs": 300 }
      ],
      "settleMs": 700
    },

    "settings-advanced": {
      "_note": "Display category scrolled down: the folded-in ADVANCED SETTINGS card sits below the DISPLAY card and is reachable.",
      "view": "qrc:/gui/PcView.qml",
      "fixtures": "default",
      "steps": [
        { "eval": "settingsButton.clicked()", "waitMs": 900 },
        { "eval": "stackView.currentItem.scrollPaneToBottom()", "waitMs": 400 }
      ],
      "settleMs": 700
    },

    "settings-input": {
      "_note": "Input category (D-pad Down x4): the INPUT card, then the folded-in GAMEPAD SETTINGS card below it.",
      "view": "qrc:/gui/PcView.qml",
      "fixtures": "default",
      "steps": [
        { "eval": "settingsButton.clicked()", "waitMs": 900 },
        { "key": "Down", "waitMs": 150 },
        { "key": "Down", "waitMs": 150 },
        { "key": "Down", "waitMs": 150 },
        { "key": "Down", "waitMs": 300 }
      ],
      "settleMs": 700
    },

    "settings-desktop": {
      "_note": "Desktop present, not Gaming Mode: the desktop-only rows (e.g. Display mode) SHOW.",
      "view": "qrc:/gui/PcView.qml",
      "fixtures": "default",
      "env": { "HAS_DESKTOP_ENVIRONMENT": "1" },
      "steps": [
        { "eval": "settingsButton.clicked()", "waitMs": 900 }
      ],
      "settleMs": 700
    },

    "settings-gaming": {
      "_note": "Desktop present but Gaming Mode forced on: the same desktop-only rows are ABSENT.",
      "view": "qrc:/gui/PcView.qml",
      "fixtures": "default",
      "env": { "HAS_DESKTOP_ENVIRONMENT": "1", "MOONVIBE_GAMING": "1" },
      "steps": [
        { "eval": "settingsButton.clicked()", "waitMs": 900 }
      ],
      "settleMs": 700
    },

    "settings-profiles": {
      "_note": "The profile bar above the rail, with a saved profile selected.",
      "view": "qrc:/gui/PcView.qml",
      "fixtures": "default",
      "steps": [
        { "eval": "settingsButton.clicked()", "waitMs": 900 },
        { "eval": "SettingsProfiles.saveAs('Balanced')", "waitMs": 300 }
      ],
      "settleMs": 700
    },

    "settings-profiles-bar-focus": {
      "_note": "Focus on the 'Save as...' button in the bar -- the accent focus ring must be visible.",
      "view": "qrc:/gui/PcView.qml",
      "fixtures": "default",
      "steps": [
        { "eval": "settingsButton.clicked()", "waitMs": 900 },
        { "eval": "SettingsProfiles.saveAs('Balanced')", "waitMs": 250 },
        { "eval": "stackView.currentItem.focusBar()", "waitMs": 150 },
        { "eval": "stackView.currentItem.focusBarControlAt(2)", "waitMs": 300 }
      ],
      "settleMs": 700
    },

    "settings-profiles-saveas": {
      "_note": "The name-a-profile dialog open, on the design system.",
      "view": "qrc:/gui/PcView.qml",
      "fixtures": "default",
      "steps": [
        { "eval": "settingsButton.clicked()", "waitMs": 900 },
        { "eval": "SettingsProfiles.saveAs('Balanced')", "waitMs": 200 },
        { "eval": "stackView.currentItem.openSaveAsForShot('Low latency')", "waitMs": 500 }
      ],
      "settleMs": 700
    },

    "settings-profiles-reset": {
      "_note": "The reset-to-defaults confirmation dialog.",
      "view": "qrc:/gui/PcView.qml",
      "fixtures": "default",
      "steps": [
        { "eval": "settingsButton.clicked()", "waitMs": 900 },
        { "eval": "stackView.currentItem.openResetForShot()", "waitMs": 500 }
      ],
      "settleMs": 700
    },

    "settings-profiles-applied": {
      "_note": "Applying a non-default profile moves the self-initializing combos live: the resolution and fps chips read 1440p / 30 FPS, and no 'modified' tag shows (the re-sync sets currentIndex without writing prefs back).",
      "view": "qrc:/gui/PcView.qml",
      "fixtures": "default",
      "steps": [
        { "eval": "settingsButton.clicked()", "waitMs": 900 },
        { "eval": "StreamingPreferences.width = 2560", "waitMs": 30 },
        { "eval": "StreamingPreferences.height = 1440", "waitMs": 30 },
        { "eval": "StreamingPreferences.fps = 30", "waitMs": 30 },
        { "eval": "StreamingPreferences.audioConfig = StreamingPreferences.AC_71_SURROUND", "waitMs": 30 },
        { "eval": "StreamingPreferences.videoCodecConfig = StreamingPreferences.VCC_FORCE_HEVC", "waitMs": 30 },
        { "eval": "SettingsProfiles.saveAs('HiFi')", "waitMs": 150 },
        { "eval": "SettingsProfiles.resetToDefaults()", "waitMs": 200 },
        { "eval": "SettingsProfiles.apply('HiFi')", "waitMs": 350 }
      ],
      "settleMs": 700
    },

    "add-pc": {
      "view": "qrc:/gui/PcView.qml",
      "fixtures": "default",
      "steps": [
        { "eval": "addPcDialog.open()", "waitMs": 500 }
      ]
    },

    "quit-confirm": {
      "view": "qrc:/gui/PcView.qml",
      "fixtures": "single",
      "steps": [
        { "eval": "quitConfirmationDialog.open()", "waitMs": 500 }
      ]
    }
  }
}
